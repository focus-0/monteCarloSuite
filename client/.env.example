# Copy to client/.env for local development (CRA loads at npm start)

# Backend API base URL — leave empty for same-origin or CRA dev proxy
REACT_APP_API_URL=

# CRA dev server proxy target (forwards /api/* when REACT_APP_API_URL is empty)
REACT_APP_PROXY_TARGET=http://localhost:5001
